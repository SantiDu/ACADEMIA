#include <iostream>
#include <iomanip>
#include <string>
#include <cmath>

using namespace std;

int main() {
     int M, T, S, x, y, a, b, c;

     M = (1 - pow(exp(1.0), 2)) + pow(x, y);
     T = x * y - a * b / (1 + pow(c, 3));
     S = a * b * (1 - 1 / c) + sqrt(5 * x - pow(y, 2));
     return 0;
}
