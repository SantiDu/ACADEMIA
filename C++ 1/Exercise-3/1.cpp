#include <iostream>
#include <iomanip>

using namespace std;

int main() {
      int order_number = 0;
      string order_date;  // mm/dd/yyyy
      double order_cost = 0.0;

      cin >> order_number >> order_date >> order_cost;
      cin >> order_number;
      cin >> order_date;
      cin >> order_cost;

      return 0;
}